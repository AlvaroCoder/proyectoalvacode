/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ["res.cloudinary.com","media.graphassets.com", "sa-east-1.graphassets.com"]
    }
};

export default nextConfig;
